# ISO Middleware - Mock Producer (.env example)
# Copy to .env and adjust values. Do NOT commit secrets.

# Middleware API base URL
MW_BASE_URL=http://127.0.0.1:8000

# Optional API key for X-API-Key header (if middleware auth is enabled)
API_KEY=

# Scenario defaults
DEFAULT_CHAIN=flare
DEFAULT_CURRENCY=FLR

# Expose a callback receiver (optional). If set, producer will pass callback_url to record-tip.
# Example: http://localhost:4001  (the service exposes POST /callback)
CALLBACK_BASE_URL=

# Port for this mock-producer service
PORT=4001
