# web-alt (Next.js) - Production Environment (Example)
# Copy these as Railway variables for the ui-service (no secrets here).

# Backend API base (the FastAPI service public URL)
NEXT_PUBLIC_API_BASE=https://api.example.com
